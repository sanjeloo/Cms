@model tbl_News
@{
    ViewData["Title"] = "Details";
}

<div class="container">
    <section class=" text-center">
        <div class="container">
            <h1>@Model.Title</h1>

            <img src="~/images/@Model.Photo" class="mw-100 m-auto mt-2 mb-2" alt="@Model.Title" />

            <p class="lead text-muted">@Html.Raw(Model.Description)</p>
            @*<p>
                    <a href="#" class="btn btn-primary my-2">Main call to action</a>
                    <a href="#" class="btn btn-secondary my-2">Secondary action</a>
                </p>*@

        </div>
    </section>
</div>


